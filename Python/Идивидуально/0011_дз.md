## 📚 ДЗ

## прочитайте и повторите перед выполнением   

[Python Циклы](https://github.com/RestartLexinus/SchoolMD/blob/main/Python/Теория/003_Python_Циклы.md)         
[Python Условия](https://github.com/RestartLexinus/SchoolMD/blob/main/Python/Теория/002_Python_Условия.md)                


---

## 🎯 Задача 1: "Проверка билета"


**Задача:**  
Напишите программу, которая проверяет, может ли человек посетить киносеанс.  
Условия:
- Возраст должен быть 12 лет или больше
- Должен быть билет (True/False)
- Если младше 18 лет, должен быть с сопровождающим (True/False)

**Требование:**  
Программа должна выводить "Можно пройти" или "Нельзя пройти" в зависимости от условий.

**Пример работы:**
```
Введите ваш возраст: 15
Есть ли у вас билет? (да/нет): да
Вы с сопровождающим? (да/нет): да
Можно пройти!
```

**Подсказки:**
1. Используйте `input()` для ввода данных
2. Преобразуйте возраст в число с помощью `int()`
3. Проверяйте условия по порядку:
   - Есть ли билет?
   - Возраст ≥ 12?
   - Если возраст < 18, есть ли сопровождающий?

**Скелет кода:**
```python
# Ввод данных
age = int(input("Введите ваш возраст: "))
has_ticket = input("Есть ли у вас билет? (да/нет): ").lower() == "да"

# Логика проверки
if not has_ticket:
    # 
elif age < 12:
    # 
elif age < 18:
    # 
```

---

## 🔁 Задача 2: "Копилка мечты"

**Задача:**  
Вы копите на новый телефон стоимостью 50000 рублей. Каждый день вы откладываете случайную сумму от 500 до 3000 рублей.  
Напишите программу, которая:
1. Симулирует процесс накопления
2. Считает, сколько дней потребуется
3. Выводит статистику каждый день

**Пример вывода:**
```
День 1: +1500 руб. | Всего: 1500 руб.
День 2: +800 руб.  | Всего: 2300 руб.
...
День 25: +1200 руб. | Всего: 50100 руб.
Поздравляем! Вы накопили на телефон за 25 дней!
```

**Подсказки:**
1. Используйте `while` цикл или `for` с `break`
2. Нужна переменная-счетчик для общей суммы
3. Используйте `random.randint(500, 3000)` для случайной суммы
4. Не забудьте импортировать модуль random

**Ключевые элементы:**
```python
import random

target = 50000  # Цель
total = 0       # Накопленная сумма
day = 0         # Счетчик дней

while total < target:
    day += 1
    total = total + random.randint(500, 3000)
    # Ваш код здесь
```

---