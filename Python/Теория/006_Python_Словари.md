📚 Шпаргалка по словарям в Python

```python
# СОЗДАНИЕ
d = {"ключ": "значение"}          # Фигурные скобки
d = dict(ключ="значение")         # Функция dict()
d = {}                            # Пустой словарь

# ДОСТУП К ДАННЫМ
значение = d["ключ"]              # Получить значение
значение = d.get("ключ")          # Безопасный доступ (если нет - None)
значение = d.get("ключ", 0)       # С значением по умолчанию

# ИЗМЕНЕНИЕ
d["ключ"] = "новое значение"      # Изменить существующее
d["новый_ключ"] = "значение"      # Добавить новое

# УДАЛЕНИЕ
del d["ключ"]                     # Удалить ключ
значение = d.pop("ключ")          # Удалить и вернуть значение

# ПРОВЕРКА
есть = "ключ" in d                # Есть ли ключ?
нет = "ключ" not in d             # Отсутствует ли ключ?

# МЕТОДЫ
ключи = d.keys()                  # Все ключи (dict_keys)
значения = d.values()             # Все значения (dict_values)
пары = d.items()                  # Все пары (dict_items)
чистая_копия = d.copy()           # Копия словаря
d.clear()                         # Очистить словарь

# ПРАВИЛА:
# 1. Ключи уникальны (повторный ключ перезапишет значение)
# 2. Ключи должны быть неизменяемыми (строки, числа, кортежи)
# 3. Значения могут быть любыми (даже другими словарями!)
# 4. Порядок сохраняется (в Python 3.7+)
```

---

📖 Полное руководство по словарям в Python

Представь, что словарь в Python — это как инвентарь в любимой игре! 🎮

В любой игре у тебя есть:

· Слоты с названиями (оружие, броня, зелья)
· Предметы в этих слотах (меч, щит, баночка здоровья)

В словаре Python работает так же:

· Ключ = название слота 🏷️
· Значение = что лежит в этом слоте 🎁

Пример из игрового мира:

```python
инвентарь_игрока = {
    "оружие": "Экскалибур",      # Ключ "оружие" → Значение "Экскалибур"
    "броня": "Драконий доспех",   # Ключ "броня" → Значение "Драконий доспех"
    "здоровье": 100,              # Ключ "здоровье" → Значение 100
    "артефакты": ["кольцо силы", "амулет мудрости"]  # Даже список может быть значением!
}
```

🔨 Создание словаря: 3 способа

Способ 1: Фигурные скобки (как создавать предметы в игре)

```python
# Создаём профиль супергероя
герой = {
    "никнейм": "КиберКот",
    "сила": 85,
    "ловкость": 90,
    "суперспособности": ["невидимость", "телепортация"],
    "снаряжение": {"костюм": "нано-броня", "оружие": "плазменная пушка"}
}
```

Способ 2: Функция dict() (более "программистский" способ)

```python
# То же самое, но пишем как команду
герой = dict(
    никнейм="КиберКот",
    сила=85,
    ловкость=90
)
```

Способ 3: Создание пустого и заполнение (с нуля!)

```python
суперзлодей = {}  # Пустой словарь - как чистый лист
суперзлодей["имя"] = "Доктор Хаос"
суперзлодей["уровень_угрозы"] = 10
суперзлодей["слабые_стороны"] = ["пауки", "громкая музыка"]
```

🔍 Доступ к данным: Как найти нужный предмет?

Прямой доступ (как открыть конкретный ящик)

```python
print(герой["никнейм"])  # Выведет: КиберКот
print(герой["сила"])     # Выведет: 85

# А что, если мы попробуем получить то, чего нет?
# print(герой["кошелёк"])  # ОШИБКА! KeyError - такого ключа нет
```

Безопасный доступ get() (проверить, есть ли предмет)

```python
# Проверяем, есть ли у героя кошелёк
кошелёк = герой.get("кошелёк")
print(кошелёк)  # Выведет: None (потому что такого ключа нет)

# Можно указать значение по умолчанию
деньги = герой.get("деньги", 0)  # Если денег нет - будет 0
print(f"Денег у героя: {деньги}")

# А теперь проверим суперспособности
способности = герой.get("суперспособности", ["нет способностей"])
print(f"Способности: {', '.join(способности)}")
```

✏️ Изменение словаря: Прокачка персонажа!

Добавление новых навыков и предметов

```python
# Герой нашёл новый артефакт!
герой["артефакт"] = "Кольцо всевидения"
print(f"Новый артефакт: {герой['артефакт']}")

# Прокачали силу после тренировки
герой["сила"] = 95  # Было 85, стало 95
print(f"Новая сила: {герой['сила']}")

# ВАЖНОЕ ПРАВИЛО: Ключи как имена - они уникальны!
герой["цвет_костюма"] = "синий"    # Сначала синий
герой["цвет_костюма"] = "чёрный"   # Перекрасили в чёрный
print(герой["цвет_костюма"])       # Выведет: чёрный (первое значение затерлось!)
```

Удаление (выбросили старый хлам)

```python
# Способ 1: del - просто выбросить
del герой["цвет_костюма"]  # Выбросили информацию о цвете

# Способ 2: pop() - выбросить, но посмотреть, что было
старое_оружие = герой.pop("оружие", "нет оружия")
print(f"Выбросили старое оружие: {старое_оружие}")

# Способ 3: clear() - полная очистка инвентаря
резервный_инвентарь = {"монеты": 50, "зелья": 3}
резервный_инвентарь.clear()  # Всё пропало!
print(резервный_инвентарь)   # {}
```

📋 Важные методы словарей: Секретные команды!

1. keys() - "Что у меня вообще есть?"

```python
игровой_профиль = {"ник": "GamerPro", "уровень": 25, "очки": 1500}

все_разделы = игровой_профиль.keys()
print(все_разделы)  # dict_keys(['ник', 'уровень', 'очки'])

# Можно пройтись по всем разделам
print("В моём профиле есть:")
for раздел in игровой_профиль.keys():
    print(f"- {раздел}")
```

2. values() - "А что внутри?"

```python
всё_содержимое = игровой_профиль.values()
print(всё_содержимое)  # dict_values(['GamerPro', 25, 1500])

# Пример: подсчёт общей стоимости предметов
магазин = {"яблоко": 10, "банан": 15, "апельсин": 12}
общая_стоимость = sum(магазин.values())
print(f"Всё стоит: {общая_стоимость} монет")
```

3. items() - "Показать всё содержимое!"

```python
# Самый полезный метод - показывает ВСЁ
for ключ, значение in игровой_профиль.items():
    print(f"{ключ}: {значение}")

# Выведет:
# ник: GamerPro
# уровень: 25
# очки: 1500
```

4. copy() - "Создать клон!"

```python
оригинальный_герой = {"имя": "Феникс", "здоровье": 100}
клон_героя = оригинальный_герой.copy()  # Создаём точную копию

# Меняем клона, оригинал не страдает
клон_героя["имя"] = "Тень Феникса"
клон_героя["способность"] = "копирование"

print("Оригинал:", оригинальный_герой)  # {'имя': 'Феникс', 'здоровье': 100}
print("Клон:", клон_героя)              # {'имя': 'Тень Феникса', 'здоровье': 100, 'способность': 'копирование'}
```

🚫 Что НЕ может быть ключом? Правила магии!

```python
# МОГУТ быть ключами (неизменяемые):
d1 = {"имя_персонажа": "Ариадна"}  # Строка ✓
d2 = {2023: "год создания"}        # Число ✓
d3 = {(1, 2): "координаты"}        # Кортеж (неизменяемый!) ✓

# НЕ МОГУТ быть ключами (изменяемые):
# d4 = {[1, 2]: "список"}         # ОШИБКА! Список можно менять
# d5 = {{}: "словарь"}            # ОШИБКА! Словарь можно менять

# Но значения могут быть ЛЮБЫМИ!
супер_словарь = {
    "список_друзей": ["Аня", "Боря", "Ваня"],  # Список как значение ✓
    "вложенный_словарь": {"секрет": "пароль"}  # Словарь в словаре ✓
}
```

🔄 Три способа перебрать словарь

```python
персонаж = {"класс": "маг", "мана": 200, "заклинания": 15}

# Способ 1: Только названия характеристик (ключи)
print("Характеристики персонажа:")
for хар-ка in персонаж:  # То же самое, что for хар-ка in персонаж.keys():
    print(f"- {хар-ка}")

# Способ 2: Всё вместе (лучший способ!)
print("\nПолная информация:")
for что, сколько in персонаж.items():
    print(f"{что}: {сколько}")

# Способ 3: Только значения (цифры/текст)
print("\nЧисловые значения:")
for значение in персонаж.values():
    print(f"- {значение}")
```

🎮 Реальный пример: Система достижений в игре

```python
# Создаём систему достижений
достижения = {
    "первый_шаг": {
        "название": "Новичок",
        "описание": "Завершить обучение",
        "награда": 100,
        "получено": True
    },
    "мастер_меча": {
        "название": "Мастер клинка",
        "описание": "Победить 100 врагов мечом",
        "награда": 500,
        "получено": False
    },
    "коллекционер": {
        "название": "Собиратель",
        "описание": "Собрать 50 разных предметов",
        "награда": 300,
        "получено": True
    }
}

# Проверяем достижения
print("=== МОИ ДОСТИЖЕНИЯ ===")
for ключ, инфо in достижения.items():
    статус = "✅" if инфо["получено"] else "❌"
    print(f"{статус} {инфо['название']}: {инфо['описание']}")

# Считаем награды
общая_награда = 0
for ключ, инфо in достижения.items():
    if инфо["получено"]:
        общая_награда += инфо["награда"]
print(f"\nВсего получено монет: {общая_награда}")

# Получаем новое достижение!
достижения["мастер_меча"]["получено"] = True
достижения["мастер_меча"]["награда"] = 550  # Бонус за скорость

# Добавляем совершенно новое достижение
достижения["неуязвимый"] = {
    "название": "Неуязвимый",
    "описание": "Пройди уровень без урона",
    "награда": 1000,
    "получено": False
}
```

💡 Полезные трюки со словарями

Трюк 1: Объединение словарей (объединяем инвентари)

```python
инвентарь1 = {"оружие": "меч", "броня": "щит"}
инвентарь2 = {"зелья": 3, "ключи": 2}

# Новый способ (Python 3.9+)
общий_инвентарь = инвентарь1 | инвентарь2
print(общий_инвентарь)  # {'оружие': 'меч', 'броня': 'щит', 'зелья': 3, 'ключи': 2}
```

Трюк 2: Словарь с значением по умолчанию

```python
from collections import defaultdict

# Автоматически создаёт список для каждого нового ключа
счетчик_монстров = defaultdict(list)
счетчик_монстров["гоблины"].append("Гоблин-воин")
счетчик_монстров["гоблины"].append("Гоблин-лучник")
счетчик_монстров["тролли"].append("Каменный тролль")

print(счетчик_монстров)
```

Трюк 3: Создание словаря из двух списков

```python
предметы = ["меч", "щит", "лук"]
качества = ["редкий", "эпический", "легендарный"]

# Два списка -> один словарь
экипировка = dict(zip(предметы, качества))
print(экипировка)  # {'меч': 'редкий', 'щит': 'эпический', 'лук': 'легендарный'}
```

🎯 Квест на закрепление: Создай свою RPG-игру!

```python
# ТВОЙ ПЕРВЫЙ ПЕРСОНАЖ
персонаж = {
    "имя": "",
    "класс": "",  # Воин, Маг, Лучник
    "уровень": 1,
    "здоровье": 100,
    "инвентарь": [],
    "навыки": {}
}

# ЗАДАНИЯ:
# 1. Заполни словарь данными своего персонажа
# 2. Добавь 3 предмета в инвентарь
# 3. Добавь 2 навыка с их уровнем (например: {"атака": 5, "защита": 3})
# 4. Повысь уровень персонажа до 5 и увеличь здоровье на 50
# 5. Напиши код, который покажет ВСЮ информацию о персонаже красиво
# 6. Создай словарь с 3 противниками
# 7. Напиши битву: сравни силу персонажа и противников

# ПОДСКАЗКА для задания 5:
print("=== КАРТА ПЕРСОНАЖА ===")
for ключ, значение in персонаж.items():
    if isinstance(значение, list):  # Если значение - список
        print(f"{ключ}: {', '.join(значение)}")
    elif isinstance(значение, dict):  # Если значение - словарь
        print(f"{ключ}:")
        for к, з in значение.items():
            print(f"  - {к}: {з}")
    else:  # Обычные значения
        print(f"{ключ}: {значение}")
```

🌟 Запомни главное:

1. Словарь = игровой инвентарь 🎒 (ключ = название ячейки, значение = предмет)
2. Ключи уникальны 🏷️ (не может быть двух ячеек с одинаковым названием)
3. items() — твой лучший друг 🤝 для просмотра всего содержимого
4. get() безопаснее, чем прямое обращение 🛡️
5. Словари могут хранить что угодно 🎁 (даже другие словари!)

