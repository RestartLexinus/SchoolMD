
## 📦 Телепорт при касании (Touched)

**Применение:** Автоматическая телепортация игрока при соприкосновении с объектом.

```lua
-- Получаем ссылку на часть, к которой прикреплен скрипт
local teleportPart = script.Parent
-- Определяем координаты точки назначения (второй части)
local destination = workspace.TeleportPoint.Position

-- Обрабатываем событие касания
teleportPart.Touched:Connect(function(hit)
    -- Получаем модель персонажа из объекта столкновения
    local character = hit.Parent
    -- Находим корневую часть персонажа
    local hrp = character:FindFirstChild("HumanoidRootPart")
    
    -- Если корневая часть существует, телепортируем персонажа
    if hrp then
        hrp.CFrame = CFrame.new(destination)
    end
end)
```

**Настройка:**
1. Создайте две части в `Workspace`
2. Вторую часть назовите `TeleportPoint`
3. Первую часть закрепите на `Anchor` и разместите в начальной точке
4. Добавьте скрипт к первой части

---

## 🖱️ Телепорт по клику (ClickDetector)

**Применение:** Телепортация по клику мыши на объект.

```lua
-- Ссылка на часть с ClickDetector
local clickPart = script.Parent
-- Координаты целевой точки
local destination = workspace.TeleportTarget.Position

-- Обрабатываем клик игрока
clickPart.ClickDetector.MouseClick:Connect(function(player)
    -- Получаем персонажа игрока
    local character = player.Character
    -- Находим корневую часть
    local hrp = character and character:FindFirstChild("HumanoidRootPart")
    
    -- Телепортируем при наличии корневой части
    if hrp then
        hrp.CFrame = CFrame.new(destination)
    end
end)
```

**Настройка:**
1. Создайте две части в разных местах
2. Вторую часть назовите `TeleportTarget`
3. К первой части добавьте `ClickDetector`
4. Присоедините скрипт к первой части

---

## ⚡ Телепорт по кнопке с ProximityPrompt

**Применение:** Интерактивная телепортация при приближении и нажатии кнопки.

```lua
-- Получаем компонент ProximityPrompt
local prompt = script.Parent.ProximityPrompt
-- Ссылка на целевую часть
local target = workspace.TeleportHere

-- Обрабатываем активацию промпта
prompt.Triggered:Connect(function(player)
    local char = player.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    
    if hrp then
        -- Телепортируем к целевой части
        hrp.CFrame = CFrame.new(target.Position)
    end
end)
```

**Настройка:**
1. Создайте две части
2. Целевую часть назовите `TeleportHere`
3. К телепортирующей части добавьте `ProximityPrompt`
4. Поместите скрипт в телепортирующую часть

---

## 🚪 Персональный телепорт (только для определенного игрока)

**Применение:** Ограниченный доступ к телепорту по имени или другим критериям.

```lua
local prompt = script.Parent.ProximityPrompt
local target = workspace.SafeZone.Position
-- Укажите имя игрока, которому разрешен доступ
local allowedPlayerName = "ВведитеИмяИгрока"

prompt.Triggered:Connect(function(player)
    -- Проверяем, имеет ли игрок доступ
    if player.Name == allowedPlayerName then
        local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = CFrame.new(target)
        end
    else
        -- Сообщение об отказе в доступе
        warn("Доступ запрещён для игрока: " .. player.Name)
    end
end)
```

**Модификации:** Вместо имени можно проверять:
- Наличие предмета в инвентаре (`player.Backpack`)
- Уровень игрока
- Роль в группе
- Значение в DataStore

---

## 🔁 Телепорт с задержкой и эффектом

**Применение:** Телепортация с паузой для создания эффекта "загрузки" или "перехода".

```lua
local part = script.Parent
local destination = workspace.TeleEnd.Position

part.Touched:Connect(function(hit)
    local hrp = hit.Parent:FindFirstChild("HumanoidRootPart")
    
    if hrp then
        -- Задержка перед телепортацией (1 секунда)
        wait(1)
        -- Выполняем телепортацию
        hrp.CFrame = CFrame.new(destination)
    end
end)
```

**Улучшения:** Во время задержки можно добавить:
- Анимацию эффектов
- Звук телепортации
- Затемнение экрана
- Сообщение о перемещении

---

## 📝 Важные рекомендации

### 1. **Используйте правильный объект для телепортации**
- **`HumanoidRootPart`** — предпочтительный выбор, так как это корневая часть модели персонажа
- Альтернативно можно использовать `Humanoid`, но это менее точно

### 2. **Точность позиционирования**
- **`CFrame.new(destination)`** — устанавливает позицию и поворот
- **`.Position`** — используется только для координат, не сохраняет ориентацию

### 3. **Безопасность телепортации**
Всегда проверяйте:
- Не телепортируйте внутрь стен или объектов
- Убедитесь, что целевая точка доступна
- Добавьте защиту от спама (cooldown)
- Обрабатывайте случай отсутствия персонажа

### 4. **Типы скриптов**
- Все представленные скрипты должны быть **`Script`**, а не `LocalScript`
- `Script` выполняется на сервере, что обеспечивает синхронизацию для всех игроков

