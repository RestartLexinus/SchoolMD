## 📚 ДЗ

## прочитайте и повторите перед выполнением   

[Python Циклы](https://github.com/RestartLexinus/SchoolMD/blob/main/Python/Теория/003_Python_Циклы.md)         
[Python Условия](https://github.com/RestartLexinus/SchoolMD/blob/main/Python/Теория/002_Python_Условия.md)                


---

### **Пример: Как получить слово «папа» разными способами срезов**

**Исходные данные:**
```python
alphabet = 'абвгдеёжзийклмнопрстуфхцчшщъыьэюя'
```

**Шаг 1: Анализируем позиции букв:**
- `'п'` → 16
- `'а'` → 0

**Шаг 2: Способы получить «папа»:**

**Способ 1: Простые срезы (базовый)**
```python
# alphabet[начало:конец]
word = alphabet[16] + alphabet[0] + alphabet[16] + alphabet[0]
print(word)  # папа
```

**Способ 3: Самый элегантный с обратным срезом**
```python

n = 1
for i in range(3):
	n = n * i
print(n)
```

---

### **Задание 1: Базовое**

Используя переменную `alphabet`, получите слово **«мама»** с помощью срезов или обращения по индексу.

**Требование:**  
Не вводите буквы `'м'` и `'а'` вручную. Весь результат должен быть собран исключительно из переменной `alphabet`.

**Подсказка:**  
- `'м'` находится на 13-й позиции (индекс 12, если считать с 0? Давайте уточним: `'а'=0`, `'б'=1`, ..., `'м'=13`)
- `'а'` находится на 0-й позиции

---

### **Задание 2: Продвинутое**

Используя переменную `alphabet`, получите фразу **«люблю маму»**.

**Особые условия:**
1. Пробела в строке `alphabet` нет
2. Для создания пробела используйте **пустой срез** (например, `alphabet[0:0]` или `alphabet[5:5]`)
3. Можно использовать конкатенацию (`+`)

**Буквы для фразы:**
- `'л'` → 12
- `'ю'` → 29
- `'б'` → 1
- `'м'` → 13
- `'а'` → 0
- `'у'` → 20

**Ожидаемый результат:**
```
люблю маму
```

**Подсказка:**  
Ваше решение будет выглядеть примерно так:  
`alphabet[12] + alphabet[29] + alphabet[1] + ... + alphabet[0:0] + ...`