
## 📚 Задачки

---

### **Задача 1: Построчный вывод символов**

**Условие:**  
Пользователь вводит произвольное сообщение. Необходимо вывести каждый символ этого сообщения на отдельной строке.

**Пример:**  
```
Введите сообщение: Hello
H
e
l
l
o
```

**Подсказки:**
- Используйте цикл `for` для перебора элементов последовательности
- Помните, что строка в Python - это последовательность символов
- Для ввода данных используйте функцию `input()`
- Каждый символ можно получить простым перебором строки

**Что проверить:**
- Работает ли с пустой строкой?
- Выводятся ли пробелы и специальные символы?
- Сохраняется ли регистр символов?

---

### **Задача 2: Сумма цифр числа**

**Условие:**  
Пользователь вводит целое число. Необходимо вычислить и вывести сумму всех цифр этого числа.

**Пример:**  
```
Введите число: 1234
Сумма цифр: 10
```

**Подсказки:**
- Используйте цикл `for` для перебора символов строки
- Функция `input()` всегда возвращает строку
- Для преобразования символа-цифры в число используйте `int()`
- Можно использовать переменную-аккумулятор для накопления суммы

**Что проверить:**
- Работает ли с однозначными числами?
- Правильно ли обрабатывается число 0?
- Что происходит с отрицательными числами?



