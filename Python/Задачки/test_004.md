🎯 Задание: Счётчик символов

Напиши программу, которая:

1. Принимает текст от пользователя
2. Подсчитывает, сколько раз встречается каждый символ в тексте
3. Выводит результат

🎯 Требования:

· Использовать словарь для хранения результатов
· Использовать цикл для перебора 

💡 Подсказки:

1. Пустой словарь создаётся так: результат = {}
2. Строку можно перебирать посимвольно в цикле
3. Проверяй, есть ли символ уже в словаре

📊 Пример работы:

```
Введите текст: привет
п: 1
р: 1
и: 1
в: 1
е: 1
т: 1
```

```
Введите текст: абба
а: 2
б: 2
```

---

💡 Небольшая подсказка по циклу и словарю

🔄 Как думать о задаче:

Представь, что у тебя есть пустая коробка с кармашками (словарь). Ты читаешь текст буква за буквой:

1. Видишь букву "а" → смотришь, есть ли кармашек "а" в коробке
   · Нет кармашка? → создаёшь кармашек "а" и кладёшь туда 1 (первая найденная)
   · Есть кармашек? → достаёшь число, прибавляешь 1, кладёшь обратно
2. Идёшь дальше по тексту → повторяешь для каждой буквы

📝 Пример мыслительного процесса:

Текст: "банан"

```python
# Начало: коробка пустая {}
# Читаем "б" → кармашка "б" нет → создаём: {"б": 1}
# Читаем "а" → кармашка "а" нет → создаём: {"б": 1, "а": 1}
# Читаем "н" → кармашка "н" нет → создаём: {"б": 1, "а": 1, "н": 1}
# Читаем "а" → кармашек "а" ЕСТЬ! → было 1, ставим 2: {"б": 1, "а": 2, "н": 1}
# Читаем "н" → кармашек "н" ЕСТЬ! → было 1, ставим 2: {"б": 1, "а": 2, "н": 2}
```

⚡ Технические намёки:

· Цикл for буква in текст: проходит по всем символам
· Проверить, есть ли ключ: if буква in словарь:
· Увеличить значение: словарь[буква] += 1
· Вывести результат: for буква, счетчик in словарь.items():
